split.py
python3 split.py --input <input directory> --output "new output directory" --ratio "train %, validation %, test %" 
splits the data into 3 directories with a given ratio
must include in/out directory and ratio
outdirectory is made on excecution



resnet50_classifier.py
python3 resnet50_classifier.py
Main script
runs resnet50 classifier 

ResNet.py
file containing renet50 definitions and constructor



tester.py
python3 tester.py 
basic testing script that takes an image and gives output
need to have better way (case switch) to display output

process_images.py 
Takes the images in the directories outputted by split.py and creates hdf5 dataset.